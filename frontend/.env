VITE_API_URL=http://localhost:5000/api
```

#### **File: `frontend/.gitignore`**

It should already exist, but verify it has:
```
node_modules
dist
.env
.env.local
```

---

### **3. Root Level `.gitignore`**

Create `.gitignore` in the `slotswapper` root:
```
node_modules/
dist/
.env
*.log
.DS_Store